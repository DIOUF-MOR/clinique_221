{% extends 'base.html.twig' %}
{% block title %}Create new RendezVous{% endblock %}

{% block body %}
    <h1>Create new RendezVous</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('add_rendez_vous') }}">Back to list</a>
{% endblock %}